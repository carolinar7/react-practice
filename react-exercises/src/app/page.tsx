import { Page } from "@/src/components/page";

export default function Home() {
  const pageContent = <div className="bg-primary w-full h-screen">h</div>

  return (
    <Page pageContent={pageContent} />
  )
}
